# A31：Fast Matrix 合同与能力规划

状态：完成（2026-08-23）。

- `ExecutionEngineConfig` 将 `auto|fast_matrix|event`、`research|formal` 与
  `config_version`、`performance.mode` 解耦。
- 旧配置缺少 `engine` 时该字段不参与序列化，V1/V2 历史 fingerprint 与默认分派不变。
- 能力规划器显式拒绝动态 sizing、风险退出、状态型约束、同 bar 风险成交、margin 饱和等
  不能证明等价的语义，并返回稳定 reason code。
- `TargetSchedule` 固定 schema、父 manifest hash、唯一键、signal/fill 顺序和完整调仓快照；
  空快照可明确表示全平。
- Fast Matrix 只使用 `fm-*` 研究身份；正式入口拒绝误用研究后端。

自动验收：`test_acceptance_31_fast_matrix_contracts.py`。
