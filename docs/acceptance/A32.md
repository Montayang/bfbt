# A32：单组合列式经济内核

状态：完成（2026-08-23）。

内核沿市场时间推进一次，横截面的数量、成本、实现/未实现盈亏、敞口和目标 delta 全部使用
Polars 原生表达式，不存在 Python 的 time×symbol 双循环。目标权重转换为真实数量，持有期间
允许权重漂移；下一调仓按开盘前真实权益和旧数量计算 delta，手续费与滑点按真实成交名义
扣除一次。

固定 fixture 覆盖 long/short、反转、漂移、next-bar-open、trade-close、手续费和滑点；共同
字段与 Event/V2 逐时点在 `1e-11` 容差内等价。

自动验收：`test_acceptance_32_fast_matrix_kernel.py`。
