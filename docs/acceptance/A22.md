# A22：全市场 24h 动量 Rank 与递减序列验收

A22 将真实策略的信号条件实现为通用配置和 O(合约数) 状态机，而不是硬编码一次性
策略。现有 `momentum` 因子用 `lookback: 24h`、`compute_interval: 1m` 即可表达因子；
本阶段新增的是跨快照 Rank 路径条件。

## 完成内容

- `portfolio.selection.mode: rank_descent`。
- `start_rank_at_least`、`entry_rank`、持平策略和上升重置均为配置。
- 严格下降与持平保留；数字 Rank 变大、缺失或离开合约池时重置。
- 到达 entry Rank 后只触发一次并消费状态。
- 状态只保存活跃候选的一行，规模 O(合约数)，支持 Parquet checkpoint 恢复。
- 状态机使用完整截面 Rank；`audit_top_n` 只裁剪发布表，不影响触发正确性。
- 新增稳定原因码 `RANK_DESCENT_TRIGGERED`。

## 专项验收

~~~bash
cd /path/to/bfbt
source .venv/bin/activate
pytest -q tests/acceptance/test_acceptance_22_rank_descent.py
~~~

预期 `9 passed`（其中四条有效路径为参数化用例）。测试覆盖 `54321`、`531`、`6421`、
持平、上升重置、缺失重置、跨块恢复、完整 Rank/Top N 分离、Rank 6 配置变化和状态硬门。

## 兼容回归

~~~bash
pytest -q \
  tests/acceptance/test_acceptance_12_v2_contracts.py \
  tests/acceptance/test_acceptance_13_exact_rank.py \
  tests/acceptance/test_acceptance_14_rank_history.py \
  tests/acceptance/test_acceptance_20_v2_chunked_engine.py \
  tests/acceptance/test_acceptance_21_streaming_publication.py \
  tests/acceptance/test_acceptance_22_rank_descent.py
~~~

## 自动验收记录

- 专项测试：`9 passed in 0.39s`。
- A12/A13/A14/A20/A21/A22 兼容回归：`54 passed in 6.45s`。
- 事件原因码扩展同步升级为 `events/v2`，run/v2 manifest 与 golden contract 已同步。
- 验收日期：2026-08-20。

本阶段不联网、不下载数据，也不执行大容量测试。
