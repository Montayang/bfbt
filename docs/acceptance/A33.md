# A33：Funding、估值与低内存分块

状态：完成（2026-08-23）。

- 支持 trade-close、mark-close 和 `(open_time, close_time]` 内真实 funding 现金流。
- funding 缺失按配置失败或置零；持仓估值可前值延续，但调仓没有真实 open 时失败，禁止
  伪造成交。
- chunked 编排对 LazyFrame 按时间过滤收集，每块只传递现金、数量、均价、最后 close、峰值
  和序号；块大小不改变逐时点结果。
- 单块行数有硬门，checkpoint 绑定完整研究 identity，错误 identity 拒绝恢复。

自动验收：`test_acceptance_33_fast_matrix_chunked.py`。
