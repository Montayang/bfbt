# A34：批量研究、不可变产物与正式提升

状态：完成（2026-08-23）。

- batch 只收集一次共享行情，各候选拥有独立现金、数量、权益和结果 hash，并设候选数硬门。
- `MatrixResearchStore` 原子发布 `fm-*`，保存 manifest、resolved config、metrics、returns、
  rebalance summary 与 TargetSchedule；路径逃逸、半发布、覆盖、碰撞和篡改均失败关闭。
- `prepare_event_promotion` 生成显式 `event/formal` 配置并保留来源 `fm-*`，不能把研究目录
  改名冒充正式 run。
- 正式 `execute_formal_run` 不接受规划为 Fast Matrix 的配置。
- `bfbt research matrix-run` 接受版本化 bars、TargetSchedule、完整调仓时点和父 hash；
  发布目录含醒目的“未经过 Event 正式确认”轻量离线报告。

自动验收：`test_acceptance_34_matrix_research.py`。
